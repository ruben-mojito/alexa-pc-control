# WoL Service environment variables
# Copy to .env and fill in real values before running.

# HTTP port
PORT=3001

# JWT secret (must match the server's JWT_SECRET)
JWT_SECRET=change-me-in-production
